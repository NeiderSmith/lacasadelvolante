/** Prefijo S3 alineado con `amplify/storage/resource.ts` (debe coincidir con la regla `recursos/categoria/*`). */
export const CATEGORY_IMAGE_S3_PREFIX = "recursos/categoria";

/** Prefijo S3 para fotos de vehículos en venta (`recursos/vehiculo/*`). */
export const VEHICLE_IMAGE_S3_PREFIX = "recursos/vehiculo";

/** Imágenes de trabajos de galería (`recursos/galeria/*`). */
export const GALLERY_WORK_IMAGE_S3_PREFIX = "recursos/galeria";
